#pragma once

#include <stdio.h>
#include <tchar.h>

#include <cstdlib>
#include <cstdint>
#include <cinttypes>
#include <ctime>
#include <chrono>
#include <stdexcept>
#include <thread>
#include <memory>

#include <string>

#include <fstream> // ifstream, ofstream
#include <iostream> // cin (getline), cout (put), cerr
#include <sstream> // stringstream

#include <array>
#include <vector> // advanced array equivalent, can use [index] subscript notation
#include <list> // sort, remove, reverse, clear, empty, push/pop frontIdx/backIdx, iterator
#include <deque> // push/pop frontIdx/backIdx

#include <utility> // pair<first, second>
#include <map> // map[key] = value, iterator<first, second>
#include <set> // set (unique), multimap

#include <algorithm>
#include <numeric>
#include <typeinfo>
